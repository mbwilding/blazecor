import type { Config } from "prettier";

const config: Config = {
    trailingComma: "all",
    arrowParens: "avoid",
    tabWidth: 4,
    useTabs: false,
    endOfLine: "lf",
    printWidth: 130,
    bracketSpacing: true,
    bracketSameLine: false,
    semi: true,
    singleQuote: false,
};

export default config;
